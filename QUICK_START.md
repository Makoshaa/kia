# 🚀 Быстрый старт - Kia Qazaqstan Dashboard

## Проверка готовности ✅

### Что уже сделано:
- ✅ База данных `kia_db` создана
- ✅ Таблицы `users` и `leads` созданы
- ✅ Пользователь admin создан (логин: admin, пароль: admin123)
- ✅ 10 тестовых лидов добавлены
- ✅ Все зависимости установлены
- ✅ Конфигурация готова

## Запуск проекта (3 простых шага)

### 1. Убедитесь, что PostgreSQL запущен

Проверьте, что PostgreSQL работает на вашем компьютере:
- Порт: 5432
- База данных: kia_db
- Пользователь: postgres
- Пароль: postgres

### 2. Запустите сервер разработки

Откройте терминал в папке проекта и выполните:

```bash
npm run dev
```

Вы увидите:
```
✓ Ready in 2.3s
- Local: http://localhost:3001
```

### 3. Откройте в браузере

Перейдите по адресу: **http://localhost:3001**

## Вход в систему 🔐

На странице входа используйте:
- **Логин:** `admin`
- **Пароль:** `admin123`

## Что вы увидите 👀

После входа откроется дашборд Kia Qazaqstan с:

### Статистика (верхняя панель)
- **Всего лидов:** 10
- **Среднее качество:** ~78%
- **Высокое качество:** 6 лидов
- **Конверсия:** 60%

### Источники трафика
- Instagram: 4
- WhatsApp: 3
- 2GIS: 3

### Таблица лидов
Полный список всех лидов с детальной информацией:
- Дата и время
- Имя клиента
- Город
- Автомобиль
- Способ покупки
- Источник
- Качество (с цветовой индикацией)
- Резюме диалога

## Автоматическое обновление ⏱️

Дашборд автоматически обновляется **каждые 60 секунд** без перезагрузки страницы.

## Тестирование API 🧪

### Добавление нового лида через PowerShell

Откройте новый терминал PowerShell и выполните:

```powershell
$body = @{
    name = "Тестовый Клиент"
    city = "Алматы"
    selected_car = "Kia Sportage"
    purchase_method = "кредит"
    client_quality = 85
    traffic_source = "Instagram"
    summary_dialog = "Это тестовый лид для проверки API"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:3001/api/leads" -Method POST -Body $body -ContentType "application/json"
```

### Проверка результата

1. Вернитесь в браузер с дашбордом
2. Подождите до 60 секунд (автообновление)
3. Новый лид появится в таблице
4. Статистика обновится

Или нажмите кнопку **"Обновить"** для немедленного обновления.

## Просмотр данных в базе 🗄️

### Через командную строку PostgreSQL:

```sql
-- Подключитесь к базе
psql -U postgres -d kia_db

-- Просмотр всех лидов
SELECT * FROM leads ORDER BY created_at DESC;

-- Просмотр пользователей
SELECT id, username, name FROM users;

-- Статистика по источникам
SELECT traffic_source, COUNT(*) as count 
FROM leads 
GROUP BY traffic_source;

-- Средний уровень качества
SELECT AVG(client_quality) as avg_quality FROM leads;
```

### Через Drizzle Studio:

```bash
npm run db:studio
```

Откроется веб-интерфейс для управления базой данных.

## Полезные команды 🛠️

```bash
# Запуск сервера разработки
npm run dev

# Сборка для production
npm run build

# Запуск production версии
npm start

# Открыть Drizzle Studio
npm run db:studio

# Применить схему к БД (если нужно)
npm run db:push

# Добавить тестовые данные заново
npx tsx scripts/seed-test-data.ts

# Создать нового пользователя
npx tsx scripts/create-user.ts
```

## Возможные проблемы и решения 🔧

### Проблема: Не могу подключиться к базе данных

**Решение:**
1. Проверьте, что PostgreSQL запущен
2. Проверьте параметры в `lib/db/index.ts`:
   - host: localhost
   - port: 5432
   - database: kia_db
   - user: postgres
   - password: postgres

### Проблема: Порт 3001 занят

**Решение:**
Next.js автоматически выберет следующий доступный порт (3002, 3003 и т.д.)
Смотрите в консоли, какой порт используется.

### Проблема: Не вижу тестовые данные

**Решение:**
```bash
# Добавьте тестовые данные заново
npx tsx scripts/seed-test-data.ts
```

### Проблема: Ошибка при входе

**Решение:**
1. Проверьте логин: `admin`
2. Проверьте пароль: `admin123`
3. Убедитесь, что пользователь создан в БД:
   ```sql
   SELECT * FROM users WHERE username = 'admin';
   ```

## Следующие шаги 📝

1. **Изучите документацию:**
   - [README_KIA.md](./README_KIA.md) - Полная документация
   - [API_DOCUMENTATION.md](./API_DOCUMENTATION.md) - API документация
   - [INTEGRATION_GUIDE.md](./INTEGRATION_GUIDE.md) - Интеграция с no-code
   - [PROJECT_SUMMARY.md](./PROJECT_SUMMARY.md) - Обзор проекта

2. **Интегрируйте с вашей no-code платформой:**
   - Настройте webhook на `http://localhost:3001/api/leads`
   - Используйте примеры из [INTEGRATION_GUIDE.md](./INTEGRATION_GUIDE.md)

3. **Подготовьтесь к развёртыванию:**
   - Выберите хостинг (Vercel, Railway, и т.д.)
   - Настройте production базу данных
   - Настройте домен

## Контакты и поддержка 📞

Если возникли вопросы:
1. Проверьте документацию в папке проекта
2. Просмотрите логи в консоли
3. Проверьте подключение к базе данных

---

## Проверочный чек-лист ✔️

Убедитесь, что всё работает:

- [ ] PostgreSQL запущен
- [ ] Сервер запустился (`npm run dev`)
- [ ] Открывается страница http://localhost:3001
- [ ] Можно войти с admin/admin123
- [ ] Видно 10 тестовых лидов
- [ ] Статистика отображается корректно
- [ ] Можно добавить лид через API
- [ ] Автообновление работает (через 60 сек)

Если все пункты отмечены - проект готов к использованию! 🎉

---

**Готово к работе!** Теперь вы можете начать интеграцию с вашей no-code платформой.
