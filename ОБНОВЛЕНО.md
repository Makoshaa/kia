# ✅ Обновление: Восстановлены старые дашборды

## Что изменилось

Проект теперь поддерживает **две системы одновременно**:

### 🚗 Новая система: Kia Qazaqstan (PostgreSQL)
- Отдельный дашборд для Kia Qazaqstan
- Хранение данных в PostgreSQL
- API для интеграции с no-code платформами
- Автоматическое обновление каждую минуту

### 📊 Старая система: Google Sheets
- Админ панель для управления дашбордами
- Управление пользователями
- Интеграция с Google Sheets через AppScript
- Мультикомпанийность

## 🔐 Учетные записи

### Kia Qazaqstan (PostgreSQL)
Показывает дашборд с лидами из PostgreSQL:

**Пользователь 1:**
- Логин: `kia`
- Пароль: `kia123`

**Пользователь 2:**
- Логин: `admin`
- Пароль: `admin123`

### Старая система (Google Sheets)

**Администратор:**
- Логин: `admin`
- Пароль: `admin`
- Доступ: Полный (управление пользователями, дашбордами)

**Пользователь 1:**
- Логин: `user1`
- Пароль: `password`
- Доступ: Просмотр назначенных дашбордов

**Пользователь 2:**
- Логин: `user2`
- Пароль: `password`
- Доступ: Просмотр назначенных дашбордов

## 🎯 Как это работает

### При входе система определяет:

1. **Если логин `kia` или `admin` (из PostgreSQL):**
   - Пытается авторизоваться через `/api/auth/login`
   - При успехе показывает **Kia Dashboard** с данными из PostgreSQL
   - Автообновление каждую минуту

2. **Если логин из старой системы (`admin`, `user1`, `user2`):**
   - Авторизация через localStorage
   - Для `admin` (старый) показывает **AdminPanel**
   - Для `user1`/`user2` показывает **UserDashboard**
   - Работа с Google Sheets

## 📂 Структура компонентов

```
components/
├── admin/                      # Админ панель (старая система)
│   ├── admin-panel.tsx
│   ├── admin-sidebar.tsx
│   ├── create-dashboard-modal.tsx
│   ├── create-user-modal.tsx
│   ├── dashboard-card.tsx
│   ├── delete-dashboard-dialog.tsx
│   ├── delete-user-dialog.tsx
│   ├── edit-dashboard-modal.tsx
│   └── edit-user-modal.tsx
├── user/                       # Пользовательские дашборды
│   └── user-dashboard.tsx
├── dashboard/                  # Просмотр дашбордов
│   └── dashboard-viewer.tsx
├── kia-dashboard.tsx          # Дашборд Kia Qazaqstan
└── auth/
    └── login-form.tsx         # Единая форма входа
```

## 🔄 Роутинг

```typescript
// app/page.tsx

if (!isAuthenticated) {
  return <LoginForm />
}

// Kia Dashboard (PostgreSQL)
if (user?.username === 'kia' || (user?.username === 'admin' && fromPostgreSQL)) {
  return <KiaDashboard />
}

// Старая система
if (isAdmin) {
  return <AdminPanel />      // Управление системой
}

return <UserDashboard />    // Просмотр дашбордов
```

## 🗄️ База данных

### PostgreSQL (Kia Qazaqstan)
```
kia_db
├── users (пользователи)
└── leads (лиды)
```

### localStorage (Старая система)
```
- user (текущий пользователь)
- dashboards (список дашбордов)
- users (список пользователей)
```

## 📡 API Endpoints

### Kia Qazaqstan (PostgreSQL)
- `POST /api/leads` - Добавить лид
- `GET /api/leads` - Получить все лиды
- `POST /api/auth/login` - Авторизация

### Старая система (Google Sheets)
- `GET /api/sheets` - Получить данные из Google Sheets
- Используется AppScript URL для каждого дашборда

## 🚀 Быстрый тест

### 1. Тест Kia Dashboard
```bash
# Запустите сервер
npm run dev

# Откройте http://localhost:3001
# Войдите: kia / kia123
# Должен открыться дашборд с 10 тестовыми лидами
```

### 2. Тест старой системы (Админ)
```bash
# Откройте http://localhost:3001
# Войдите: admin / admin
# Должна открыться админ панель с управлением
```

### 3. Тест старой системы (Пользователь)
```bash
# Откройте http://localhost:3001
# Войдите: user1 / password
# Должен открыться пользовательский дашборд
```

## ⚙️ Настройка

### Для Kia Qazaqstan:
1. База данных уже настроена
2. 2 пользователя созданы (kia, admin)
3. 10 тестовых лидов загружены
4. API готов к интеграции

### Для старой системы:
1. Создайте дашборд в админ панели
2. Укажите URL Google Sheets
3. Назначьте владельца дашборда
4. Пользователь сможет просматривать свои дашборды

## 📝 Что можно делать

### Как Kia (kia / kia123):
- ✅ Просмотр всех лидов из PostgreSQL
- ✅ Статистика в реальном времени
- ✅ Автообновление каждую минуту
- ❌ Нет управления системой

### Как Admin (admin / admin):
- ✅ Создание пользователей
- ✅ Создание дашбордов
- ✅ Управление доступом
- ✅ Редактирование/удаление
- ❌ Нет доступа к Kia лидам

### Как User (user1, user2):
- ✅ Просмотр назначенных дашбордов
- ✅ Просмотр данных из Google Sheets
- ❌ Нет управления системой
- ❌ Нет доступа к Kia лидам

## 🔗 Связь между системами

**Системы работают независимо:**
- Kia Dashboard → PostgreSQL → `/api/leads`
- Старые дашборды → Google Sheets → AppScript

**Общее:**
- Единая форма входа
- Общий роутинг
- Общие UI компоненты

## 📚 Документация

### Для Kia Qazaqstan:
- [API_DOCUMENTATION.md](./API_DOCUMENTATION.md)
- [INTEGRATION_GUIDE.md](./INTEGRATION_GUIDE.md)
- [QUICK_START.md](./QUICK_START.md)

### Для старой системы:
- Функционал сохранён как был
- Google Sheets интеграция работает
- AppScript endpoints работают

## 🎉 Итого

Теперь у вас **два дашборда в одном проекте**:

1. **Современный** - Kia Qazaqstan с PostgreSQL
2. **Классический** - Старая система с Google Sheets

Оба работают одновременно, не мешая друг другу!

---

**Готово к использованию!** 🚀
